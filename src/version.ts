/**
 * The version of this package. Kept in sync with `package.json` (enforced by
 * a unit test). Lives in its own module so the client can stamp it into the
 * `x-slothbox-sdk` identification header without importing the package
 * entrypoint.
 */
export const VERSION = '0.1.0';
